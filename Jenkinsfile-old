pipeline {
    agent {
        label 'Maven'
    }
    tools {
        jdk 'Home_java'
        maven 'maven'
    } 
    stages {
        stage('test'){
            steps {
                sh "mvn clean test surefire-report:report-only"
            }
        }
    }
}