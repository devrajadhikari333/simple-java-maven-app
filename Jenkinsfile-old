pipeline {
    agent {
        label 'Maven'
    }
    tools {
        jdk 'openjdk8'
        maven 'maven'
    } 
    stages {
        stage('test'){
            steps {
                sh "mvn clean test surefire-report:report-only"
            }
        }
    }
}