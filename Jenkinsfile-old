pipeline {
    agent {
        label 'Maven'
    }
    tools {
        Home_Java 'openjdk8'
        maven 'maven'
    } 
    stages {
        stage('test'){
            steps {
                sh "mvn clean test surefire-report:report-only"
            }
        }
    }
}