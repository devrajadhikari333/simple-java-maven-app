pipeline {
    agents {
        label 'Maven'
    }
    tools {
        jdk 'openjdk8'
        maven 'maven'
    } 
    stages ('test'){
        steps {
            sh "mvn clean test surefire-report:report-only"
        }
    }
}