pipeline {
    agent {
        label 'Maven'
    }
    tools {
        maven 'maven'
    } 
    stages {
        stage('test'){
            steps {
                sh "mvn clean test surefire-report:report-only"
            }
        }
    }
}